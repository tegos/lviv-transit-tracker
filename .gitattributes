# Normalise line endings: LF in the repo, LF on checkout everywhere.
* text=auto eol=lf

# Binary assets — never touched.
*.png binary
*.jpg binary
*.webp binary
*.ico binary
*.woff binary
*.woff2 binary
